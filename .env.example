# Server side (Netlify functions) — never exposed to the browser
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-haiku-4-5

# Who the replies are signed as, and where to send people to book (optional)
SENDER_NAME=Ahmed
SENDER_TITLE=Executive Director, Trustpoint Xposure
BOOKING_LINK=

# Client side (baked into the bundle at build time) — static login credentials
VITE_APP_USER=admin
VITE_APP_PASS=humanize2026
